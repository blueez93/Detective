modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license} (see LICENSE)"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="Blue"
description='''
Client-side modpack diagnostics that detect performance stalls, preserve nearby performance history, and present cautious evidence about probable mod suspects.
''' 

[[dependencies.${mod_id}]]
modId="neoforge"
type="required"
versionRange="[${neo_min_version},)"
ordering="NONE"
side="CLIENT"

[[dependencies.${mod_id}]]
modId="minecraft"
type="required"
versionRange="${minecraft_version_range}"
ordering="NONE"
side="CLIENT"
